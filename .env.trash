# --- Worker HTTP + sécurité ---
CRAWLER_WORKER_PORT=8080
CRAWLER_WORKER_SECRET=crawler_worker_7f3fca91f20e4f7c8b7c9e5f1f3a2e89   # (ta clé partagée)
ALLOWED_ORIGINS=*
LOG_LEVEL=info

# --- URL publique du worker (vue par l’Edge Function) ---
# Si tu exposes le worker public via ton domaine:
CRAWLER_WORKER_URL=https://puppeteer.evaleads.com/crawl4ai
# (Optionnel) si tu testes en local: http://host.docker.internal:8080  (Mac/Win)
# ou http://<IP-de-ta-machine>:8080 (Linux)

